# 🧪 Тестирование проекта

Этот документ описывает, как запускать тесты для проекта VK Bot.

## 📦 Установка зависимостей

Перед запуском тестов необходимо установить зависимости:

```bash
# Установка зависимостей для backend
npm install

# Установка зависимостей для frontend
cd max-web
npm install
cd ..
```

## 📋 Структура тестов

Проект использует **Vitest** для тестирования как backend, так и frontend кода.

### Backend тесты
- Расположены в директории `tests/`
- Тестируют API endpoints, database utilities и bot handlers
- Используют моки для изоляции тестов

### Frontend тесты
- Расположены рядом с компонентами (например, `src/components/SearchBar.test.tsx`)
- Тестируют React компоненты и утилиты
- Используют React Testing Library и jsdom

## 🚀 Запуск тестов

### Backend тесты

Из корневой директории проекта:

```bash
# Запустить все тесты один раз
npm test

# Запустить тесты в watch режиме (автоматически перезапускаются при изменениях)
npm run test:watch

# Запустить тесты с UI интерфейсом
npm run test:ui

# Запустить тесты с покрытием кода
npm run test:coverage
```

### Frontend тесты

Из директории `max-web/`:

```bash
cd max-web

# Запустить все тесты один раз
npm test

# Запустить тесты в watch режиме
npm run test:watch

# Запустить тесты с UI интерфейсом
npm run test:ui

# Запустить тесты с покрытием кода
npm run test:coverage
```

### Запуск всех тестов

Для запуска всех тестов (backend + frontend) из корневой директории:

```bash
# Backend тесты
npm test

# Frontend тесты
cd max-web && npm test && cd ..
```

## 📊 Покрытие кода

После запуска тестов с флагом `--coverage`, отчеты о покрытии будут сгенерированы в:
- Backend: `coverage/` в корне проекта
- Frontend: `max-web/coverage/`

Откройте `coverage/index.html` в браузере для просмотра детального отчета.

## 📝 Написание новых тестов

### Backend тесты

Создайте файл с расширением `.test.ts` в директории `tests/`:

```typescript
import { describe, it, expect, vi } from 'vitest';

describe('MyFunction', () => {
  it('должен делать что-то', () => {
    // ваш тест
  });
});
```

### Frontend тесты

Создайте файл с расширением `.test.tsx` рядом с тестируемым компонентом:

```typescript
import { describe, it, expect } from 'vitest';
import { render, screen } from '@testing-library/react';
import { MyComponent } from './MyComponent';

describe('MyComponent', () => {
  it('должен отображать контент', () => {
    render(<MyComponent />);
    expect(screen.getByText('Hello')).toBeInTheDocument();
  });
});
```

## 🔧 Конфигурация

### Backend
Конфигурация находится в `vitest.config.ts` в корне проекта.

### Frontend
Конфигурация находится в `max-web/vitest.config.ts`.

## 📦 Зависимости для тестирования

### Backend
- `vitest` - тестовый фреймворк
- `@vitest/ui` - UI интерфейс для тестов

### Frontend
- `vitest` - тестовый фреймворк
- `@vitest/ui` - UI интерфейс для тестов
- `@testing-library/react` - утилиты для тестирования React компонентов
- `@testing-library/jest-dom` - дополнительные matchers для DOM
- `@testing-library/user-event` - симуляция пользовательских событий
- `jsdom` - DOM окружение для тестов

## 🐛 Отладка тестов

### Запуск конкретного теста

```bash
# Backend
npm test -- tests/api/create-max-card.test.ts

# Frontend
cd max-web && npm test -- src/components/SearchBar.test.tsx
```

### Запуск тестов в определенном файле

```bash
# Backend
npm test -- create-max-card

# Frontend
cd max-web && npm test -- SearchBar
```

### Просмотр логов

Используйте `console.log` или `console.error` в тестах - они будут видны в выводе.

## ✅ Рекомендации

1. **Пишите тесты для критичной логики** - API endpoints, database operations, бизнес-логика
2. **Используйте моки** для изоляции тестов от внешних зависимостей (БД, API)
3. **Тестируйте edge cases** - пустые значения, ошибки, граничные условия
4. **Поддерживайте тесты актуальными** - обновляйте тесты при изменении кода
5. **Стремитесь к хорошему покрытию** - но не гонитесь за 100%, фокус на важной логике

## 📚 Дополнительные ресурсы

- [Vitest документация](https://vitest.dev/)
- [React Testing Library](https://testing-library.com/react)
- [Testing Best Practices](https://kentcdodds.com/blog/common-mistakes-with-react-testing-library)

